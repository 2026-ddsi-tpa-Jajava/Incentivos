$kind: http-request
name: POST-misiones-donadorID
method: POST
url: '{{baseUrl}}/misiones/{{donadorID}}'
description: Asignar misión a un donador
pathVariables:
  - key: donadorID
    value: '{{donadorID}}'
body:
  type: json
  content: |-
    {
      "misionID": "string"
    }
