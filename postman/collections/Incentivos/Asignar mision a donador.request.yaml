$kind: http-request
description: Asignar misión a un donador
url: "{{baseUrl}}/misiones/{{donadorID}}"
method: POST
pathVariables:
  donadorID: "{{donadorID}}"
body:
  type: json
  content: |-
    {
      "misionID": "mis-1"
    }
order: 2000
